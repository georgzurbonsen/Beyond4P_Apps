Function Name;Function Type;Min Param;Max Param;Para Repetition;Para #;Para Name;Para Direction;Para Type Expected;Para Type Addtl Info
10p;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
abbreviate name;Function;1;1;1
;;;;;1;full name;Input;literal;Literal
abort;Procedure;0;1;1
;;;;;1;return value or exit code;Input;numeral;Numeral
abs;Function;1;1;1
;;;;;1;value;Input;all;Value of any type
acos;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
acos deg;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
acosh;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
add;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
advanced directory listing;Both;1;4;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;path name;Input;literal;Literal
;;;;;3;match name;Input;literal;Literal
;;;;;4;listing option;Input;literal;Literal
advanced directory listing recursive;Both;1;4;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;path name;Input;literal;Literal
;;;;;3;match name;Input;literal;Literal
;;;;;4;listing option;Input;literal;Literal
and;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
array;Procedure;2;99999999;2
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;initial values;Input;parameters;Parameter sets
array protect;Procedure;3;99999999;2
;;;;;1;protection level;Input;literal;Literal
;;;;;2;variable name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;3;initial values;Input;parameters;Parameter sets
array to structure;Procedure;1;2;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;member names;Input;valid;Numeral, Literal, Boolean, Date, Parameter
array to structure recursive;Procedure;1;2;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;member names;Input;valid;Numeral, Literal, Boolean, Date, Parameter
asin;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
asin deg;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
asinh;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
assign;Procedure;2;2;1
;;;;;1;destination variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;value;Input;all;Value of any type
atan;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
atan deg;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
atanh;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
attribute delete;Both;3;3;1
;;;;;1;path name;Input;literal;Literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;attribute name(s);Input;to parameters members to literal;Valid types are converted to parameters, members to literal
attribute listing;Both;4;4;1
;;;;;1;path name;Input;literal;Literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;attribute name(s);Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;attribute value;Output;all;Value of any type
attribute modify;Both;4;4;1
;;;;;1;path name;Input;literal;Literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;attribute name(s);Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;attribute value;Input;valid;Numeral, Literal, Boolean, Date, Parameter
attribute read;Both;4;4;1
;;;;;1;path name;Input;literal;Literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;attribute name(s);Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;attribute value;Output;all;Value of any type
attribute remove;Both;3;3;1
;;;;;1;path name;Input;literal;Literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;attribute name(s);Input;to parameters members to literal;Valid types are converted to parameters, members to literal
attribute write;Both;4;4;1
;;;;;1;path name;Input;literal;Literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;attribute name(s);Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;attribute value;Input;valid;Numeral, Literal, Boolean, Date, Parameter
average;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
average if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
average if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
average ignore blanks;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
average ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
average ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
average ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
average ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
average ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
background color;Both;0;1;1
;;;;;1;color;Input;literal;Literal
boolean;Function;1;1;1
;;;;;1;value;Input;valid;Numeral, Literal, Boolean, Date, Parameter
break;Procedure;0;0;1
break case;Procedure;0;0;1
break loop;Procedure;0;0;1
cagr;Function;1;3;1
;;;;;1;Values;Input;parameters;Parameter sets
;;;;;2;Time base;Input;numeral;Numeral
;;;;;3;Grouping;Input;literal;Literal
call;Both;2;2;1
;;;;;1;function name;Input;literal;Literal
;;;;;2;function parameters;Input;parameters;Parameter sets
case;Procedure;1;1;1
;;;;;1;applicable values;Code piece;comparison expression;Code piece or :literal
catch;Procedure;0;1;1
;;;;;1;variable for return value;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
catch if;Procedure;2;2;1
;;;;;1;destination variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
check;Procedure;1;1;1
;;;;;1;expression;Input;numeral or literal or date literal;Literal Version of Numeral/Date created
choose recent file;Function;5;5;1
;;;;;1;prompt;Input;literal;Literal
;;;;;2;path;Input;literal;Literal
;;;;;3;match;Input;literal;Literal
;;;;;4;ordering criteria;Input;literal;Literal
;;;;;5;count;Input;numeral;Numeral
chr;Function;1;1;1
;;;;;1;character code;Input;numeral;Numeral
clean if numeral;Function;1;2;1
;;;;;1;value;Input;numeral or literal;Literal version of numeral created
;;;;;2;decimal symbol;Input;literal;Literal
clean numeral;Function;1;2;1
;;;;;1;value;Input;numeral or literal;Literal version of numeral created
;;;;;2;decimal symbol;Input;literal;Literal
cls;Procedure;0;0;1
code;Function;1;2;1
;;;;;1;input string;Input;literal;Literal
;;;;;2;character position;Input;numeral;Numeral
compare;Function;2;2;1
;;;;;1;value;Input;all;Value of any type
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
compare pick;Function;4;99999999;2
;;;;;1;value to compare;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;expression if all false;Code piece;expression;Code piece or :literal
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;4;expression if true;Code piece;expression;Code piece or :literal
compare select;Function;4;99999999;2
;;;;;1;value to compare;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;value if all false;Input;all;Value of any type
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;4;value if true;Input;all;Value of any type
compose;Both;0;99999999;1
;;;;;1;values;Input;to literal;Valid value of any type which is converted to literal
compose line;Both;0;99999999;1
;;;;;1;values;Input;to literal;Valid value of any type which is converted to literal
continue;Procedure;0;0;1
continue case;Procedure;0;0;1
continue loop;Procedure;0;0;1
continue with next case;Procedure;0;0;1
correlation;Function;2;2;1
;;;;;1;1st vector;Input;parameters;Parameter sets
;;;;;2;2nd vector;Input;parameters;Parameter sets
cos;Function;1;1;1
;;;;;1;angle in radians;Input;numeral;Numeral
cos deg;Function;1;1;1
;;;;;1;angle in degrees;Input;numeral;Numeral
cosh;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
count;Function;1;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
count elements;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;value to match;Input;valid;Numeral, Literal, Boolean, Date, Parameter
count elements ignore blanks;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;value to match;Input;valid;Numeral, Literal, Boolean, Date, Parameter
count elements ignore both;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;value to match;Input;valid;Numeral, Literal, Boolean, Date, Parameter
count elements ignore case;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;value to match;Input;valid;Numeral, Literal, Boolean, Date, Parameter
count files;Function;0;4;1
;;;;;1;path name;Input;literal;Literal
;;;;;2;match name;Input;literal;Literal
;;;;;3;match count limitation;Input;numeral;Numeral
;;;;;4;search options;Input;literal;Literal
count files recursive;Function;0;4;1
;;;;;1;path name;Input;literal;Literal
;;;;;2;match name;Input;literal;Literal
;;;;;3;match count limitation;Input;numeral;Numeral
;;;;;4;search options;Input;literal;Literal
count if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
count if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
count ignore blanks;Function;1;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
count ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
count ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
count ignore both;Function;1;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
count ignore both if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
count ignore both if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
count ignore zero;Function;1;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
count ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
count ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
covariance;Function;2;2;1
;;;;;1;1st vector;Input;parameters;Parameter sets
;;;;;2;2nd vector;Input;parameters;Parameter sets
cursor;Procedure;2;2;1
;;;;;1;x;Input;numeral;Numeral
;;;;;2;y;Input;numeral;Numeral
date;Function;1;6;1
;;;;;1;date/time or year;Input;numeral or literal or date;Numerals, literals or dates
;;;;;2;month or date format;Input;numeral;Numeral
;;;;;3;day;Input;numeral;Numeral
;;;;;4;hour;Input;numeral;Numeral
;;;;;5;minute;Input;numeral;Numeral
;;;;;6;second;Input;numeral;Numeral
date inside;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
date time;Function;1;6;1
;;;;;1;date/time or year;Input;numeral or literal or date;Numerals, literals or dates
;;;;;2;month or date format;Input;numeral;Numeral
;;;;;3;day;Input;numeral;Numeral
;;;;;4;hour;Input;numeral;Numeral
;;;;;5;minute;Input;numeral;Numeral
;;;;;6;second;Input;numeral;Numeral
day;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
day of year;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
decode entities;Function;1;1;1
;;;;;1;input literal;Input;literal;Literal
deep;Both;2;4;1
;;;;;1;function name;Input;literal;Literal
;;;;;2;function parameters;Input;parameters;Parameter sets
;;;;;3;depth;Input;numeral;Numeral
;;;;;4;padding value;Input;valid;Numeral, Literal, Boolean, Date, Parameter
deepr;Both;2;4;1
;;;;;1;function name;Input;literal;Literal
;;;;;2;function parameters;Input;parameters;Parameter sets
;;;;;3;depth;Input;numeral;Numeral
;;;;;4;padding value;Input;valid;Numeral, Literal, Boolean, Date, Parameter
define additional function;Procedure;2;6;1
;;;;;1;new name;Input;literal;Literal
;;;;;2;existing name;Input;literal;Literal
;;;;;3;parameter declaration;Input;parameters;Parameter sets
;;;;;4;minimum parameter count;Input;numeral;Numeral
;;;;;5;maximum parameter count;Input;numeral or literal;Literal version of numeral created
;;;;;6;last param repetition;Input;numeral;Numeral
define additional procedure;Procedure;2;6;1
;;;;;1;new name;Input;literal;Literal
;;;;;2;existing name;Input;literal;Literal
;;;;;3;parameter declaration;Input;parameters;Parameter sets
;;;;;4;minimum parameter count;Input;numeral;Numeral
;;;;;5;maximum parameter count;Input;numeral or literal;Literal version of numeral created
;;;;;6;last param repetition;Input;numeral;Numeral
define additional procedure and function;Procedure;2;6;1
;;;;;1;new name;Input;literal;Literal
;;;;;2;existing name;Input;literal;Literal
;;;;;3;parameter declaration;Input;parameters;Parameter sets
;;;;;4;minimum parameter count;Input;numeral;Numeral
;;;;;5;maximum parameter count;Input;numeral or literal;Literal version of numeral created
;;;;;6;last param repetition;Input;numeral;Numeral
define function;Procedure;1;5;1
;;;;;1;new name;Input;literal;Literal
;;;;;2;parameter declaration;Input;parameters;Parameter sets
;;;;;3;minimum parameter count;Input;numeral;Numeral
;;;;;4;maximum parameter count;Input;numeral or literal;Literal version of numeral created
;;;;;5;last param repetition;Input;numeral;Numeral
define procedure;Procedure;1;5;1
;;;;;1;new name;Input;literal;Literal
;;;;;2;parameter declaration;Input;parameters;Parameter sets
;;;;;3;minimum parameter count;Input;numeral;Numeral
;;;;;4;maximum parameter count;Input;numeral or literal;Literal version of numeral created
;;;;;5;last param repetition;Input;numeral;Numeral
define procedure and function;Procedure;1;5;1
;;;;;1;new name;Input;literal;Literal
;;;;;2;parameter declaration;Input;parameters;Parameter sets
;;;;;3;minimum parameter count;Input;numeral;Numeral
;;;;;4;maximum parameter count;Input;numeral or literal;Literal version of numeral created
;;;;;5;last param repetition;Input;numeral;Numeral
delete;Procedure;0;99999999;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
delete members;Procedure;0;99999999;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
delete user function;Procedure;1;1;1
;;;;;1;name of existing user function;Input;literal;Literal
deviation;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
deviation if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
deviation if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
deviation ignore blanks;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
deviation ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
deviation ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
deviation ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
deviation ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
deviation ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
diagonal;Function;1;2;1
;;;;;1;input vector;Input;parameters;Parameter sets
;;;;;2;values outside diagonal;Input;valid;Numeral, Literal, Boolean, Date, Parameter
dim;Procedure;3;99999999;3
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;dimensions;Input;numeral or literal or date literal;Literal Version of Numeral/Date created
;;;;;3;initial value;Input;all;Value of any type
dim protect;Procedure;4;99999999;3
;;;;;1;protection setting;Input;literal;Literal
;;;;;2;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;3;dimensions;Input;numeral or literal or date literal;Literal Version of Numeral/Date created
;;;;;4;initial value;Input;all;Value of any type
directory create;Both;0;99999999;1
;;;;;1;directory name;Input;literal;Literal
directory create all;Both;0;99999999;1
;;;;;1;directory name;Input;literal;Literal
directory create silently;Both;0;99999999;1
;;;;;1;directory name;Input;literal;Literal
directory delete;Both;0;99999999;1
;;;;;1;name of existing directory;Input;literal;Literal
directory delete all;Both;0;99999999;1
;;;;;1;name of existing directory;Input;literal;Literal
directory delete multiple;Both;1;2;1
;;;;;1;Directory path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
directory delete multiple all;Both;1;2;1
;;;;;1;Directory path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
directory delete recursive;Both;1;2;1
;;;;;1;Directory path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
directory delete recursive all;Both;1;2;1
;;;;;1;Directory path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
directory delete recursive if empty;Both;1;2;1
;;;;;1;Directory path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
directory delete silently;Both;0;99999999;1
;;;;;1;name of existing directory;Input;literal;Literal
directory existing;Function;1;1;1
;;;;;1;directory name;Input;literal;Literal
directory listing;Both;1;4;1
;;;;;1;name of table;Input;literal;Literal
;;;;;2;path name;Input;literal;Literal
;;;;;3;match name;Input;literal;Literal
;;;;;4;search option;Input;literal;Literal
directory listing recursive;Both;1;4;1
;;;;;1;name of table;Input;literal;Literal
;;;;;2;path name;Input;literal;Literal
;;;;;3;match name;Input;literal;Literal
;;;;;4;search option;Input;literal;Literal
directory rename;Both;2;2;1
;;;;;1;old directory names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;new directory names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
directory rename all;Both;2;2;1
;;;;;1;old directory names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;new directory names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
directory rename silently;Both;2;2;1
;;;;;1;old directory names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;new directory names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
directory size;Function;1;1;1
;;;;;1;file or path name;Input;literal;Literal
discount;Procedure;2;3;1
;;;;;1;values to discount;I/O;parameters;Parameter sets
;;;;;2;WACC;Input;numeral;Numeral
;;;;;3;time base;Input;literal;Literal
disk space;Function;1;1;1
;;;;;1;drive or directory name;Input;literal;Literal
disk space free;Function;1;1;1
;;;;;1;drive or directory name;Input;literal;Literal
distribute;Both;10;14;1
;;;;;1;total value to distribute;Input;numeral or literal;Literal version of numeral created
;;;;;2;batch size;Input;numeral;Numeral
;;;;;3;start date;Input;to date;Values which are dates or can be converted to dates
;;;;;4;end date or daily capacity;Input;numeral or literal or date;Numerals, literals or dates
;;;;;5;intervals;Input;literal;Literal
;;;;;6;usance;Input;literal;Literal
;;;;;7;output start date;Input;to date;Values which are dates or can be converted to dates
;;;;;8;output end date;Input;to date;Values which are dates or can be converted to dates
;;;;;9;timeline start dates;Output;parameters;Parameter sets
;;;;;10;distributed values;Output;parameters;Parameter sets
;;;;;11;earlier sum;Output;numeral;Numeral
;;;;;12;later sum;Output;numeral;Numeral
;;;;;13;bank holidays;Input;parameters;Parameter sets
;;;;;14;school holidays;Input;parameters;Parameter sets
distribute advance;Both;10;14;1
;;;;;1;total value to distribute;Input;numeral or literal;Literal version of numeral created
;;;;;2;batch size;Input;numeral;Numeral
;;;;;3;start date;Input;to date;Values which are dates or can be converted to dates
;;;;;4;end date or daily capacity;Input;numeral or literal or date;Numerals, literals or dates
;;;;;5;intervals;Input;literal;Literal
;;;;;6;usance;Input;literal;Literal
;;;;;7;output start date;Input;to date;Values which are dates or can be converted to dates
;;;;;8;output end date;Input;to date;Values which are dates or can be converted to dates
;;;;;9;timeline start dates;Output;parameters;Parameter sets
;;;;;10;distributed values;Output;parameters;Parameter sets
;;;;;11;earlier sum;Output;numeral;Numeral
;;;;;12;later sum;Output;numeral;Numeral
;;;;;13;bank holidays;Input;parameters;Parameter sets
;;;;;14;school holidays;Input;parameters;Parameter sets
div;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
do;Procedure;0;0;1
docs;Procedure;0;0;1
dump functions;Procedure;1;1;1
;;;;;1;new table name;Input;literal;Literal
echo;Both;0;99999999;1
;;;;;1;values;Input;to literal;Valid value of any type which is converted to literal
encode entities;Function;1;2;1
;;;;;1;input string;Input;literal;Literal
;;;;;2;use new line in html format;Input;boolean;Boolean type
end;Procedure;0;1;1
;;;;;1;return value or exit code;Input;numeral;Numeral
even;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
excel column number;Function;1;1;1
;;;;;1;alphabetic column specification;Input;literal;Literal
excel list sheets;Function;1;1;1
;;;;;1;excel file name;Input;literal;Literal
exception;Procedure;2;3;1
;;;;;1;action;Input;literal;Literal
;;;;;2;message;Input;literal;Literal
;;;;;3;up in code hierarchy;Input;numeral;Numeral
exchange;Procedure;2;2;1
;;;;;1;variable or table entry 1;I/O;all;Value of any type
;;;;;2;variable or table entry 2;I/O;all;Value of any type
existing;Function;1;1;1
;;;;;1;variable name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
exit;Procedure;0;1;1
;;;;;1;return value or exit code;Input;numeral;Numeral
exp;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
explain functions;Procedure;0;1;1
;;;;;1;function name or wildcard;Input;literal;Literal
expression;Both;1;1;1
;;;;;1;expression;Code piece;expression;Code piece or :literal
file copy;Both;2;2;1
;;;;;1;names of existing files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy all;Both;2;2;1
;;;;;1;names of existing files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy multiple;Both;2;3;1
;;;;;1;Source path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;Destination path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy multiple all;Both;2;3;1
;;;;;1;Source path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;Destination path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy multiple overwrite;Both;2;3;1
;;;;;1;Source path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;Destination path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy multiple overwrite all;Both;2;3;1
;;;;;1;Source path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;Destination path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy overwrite;Both;2;2;1
;;;;;1;names of existing files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy overwrite all;Both;2;2;1
;;;;;1;names of existing files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy overwrite silently;Both;2;2;1
;;;;;1;names of existing files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy recursive;Both;2;3;1
;;;;;1;Source path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;Destination path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy recursive all;Both;2;3;1
;;;;;1;Source path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;Destination path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy recursive overwrite;Both;2;3;1
;;;;;1;Source path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;Destination path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy recursive overwrite all;Both;2;3;1
;;;;;1;Source path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;Destination path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file copy silently;Both;2;2;1
;;;;;1;names of existing files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file create link;Both;2;2;1
;;;;;1;symbolic link file name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;target file or directory name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file create link all;Both;2;2;1
;;;;;1;symbolic link file name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;target file or directory name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file create link silently;Both;2;2;1
;;;;;1;symbolic link file name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;target file or directory name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file delete;Both;0;99999999;1
;;;;;1;file name;Input;literal;Literal
file delete all;Both;0;99999999;1
;;;;;1;file name;Input;literal;Literal
file delete multiple;Both;1;2;1
;;;;;1;Directory path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file delete multiple all;Both;1;2;1
;;;;;1;Directory path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file delete recursive;Both;1;2;1
;;;;;1;Directory path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file delete recursive all;Both;1;2;1
;;;;;1;Directory path name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;File or pattern name;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file delete silently;Both;0;99999999;1
;;;;;1;file name;Input;literal;Literal
file download;Both;2;2;1
;;;;;1;URL names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file download all;Both;2;2;1
;;;;;1;URL names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file download overwrite;Both;2;2;1
;;;;;1;URL names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file download overwrite all;Both;2;2;1
;;;;;1;URL names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file download overwrite silently;Both;2;2;1
;;;;;1;URL names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file download silently;Both;2;2;1
;;;;;1;URL names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;names of new files;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file existing;Function;1;1;1
;;;;;1;file name;Input;literal;Literal
file rename;Both;2;2;1
;;;;;1;old file names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;new file names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file rename all;Both;2;2;1
;;;;;1;old file names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;new file names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file rename silently;Both;2;2;1
;;;;;1;old file names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;new file names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
file size;Function;1;1;1
;;;;;1;file or path name;Input;literal;Literal
filter;Both;2;99999999;1
;;;;;1;parameter set to filter;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;follower;I/O;parameters;Parameter sets
filter 1;Both;2;99999999;1
;;;;;1;parameter set to filter;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;follower;I/O;parameters;Parameter sets
filter n;Both;3;99999999;1
;;;;;1;parameter set to filter;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;count;Input;numeral;Numeral
;;;;;4;follower;I/O;parameters;Parameter sets
filter start;Both;2;99999999;1
;;;;;1;parameter set to filter;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;follower;I/O;parameters;Parameter sets
filter start include;Both;2;99999999;1
;;;;;1;parameter set to filter;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;follower;I/O;parameters;Parameter sets
filter start n;Both;3;99999999;1
;;;;;1;parameter set to filter;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;count;Input;numeral;Numeral
;;;;;4;follower;I/O;parameters;Parameter sets
filter start n include;Both;3;99999999;1
;;;;;1;parameter set to filter;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;count;Input;numeral;Numeral
;;;;;4;follower;I/O;parameters;Parameter sets
filter stop;Both;2;99999999;1
;;;;;1;parameter set to filter;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;follower;I/O;parameters;Parameter sets
filter stop include;Both;2;99999999;1
;;;;;1;parameter set to filter;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;follower;I/O;parameters;Parameter sets
find;Function;2;4;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;input pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;starting position;Input;numeral;Numeral
;;;;;4;pattern number;Output;numeral;Numeral
find ignore blanks;Function;2;4;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;input pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;starting position;Input;numeral;Numeral
;;;;;4;pattern number;Output;numeral;Numeral
find ignore both;Function;2;4;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;input pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;starting position;Input;numeral;Numeral
;;;;;4;pattern number;Output;numeral;Numeral
find ignore case;Function;2;4;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;input pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;starting position;Input;numeral;Numeral
;;;;;4;pattern number;Output;numeral;Numeral
flat;Function;1;1;1
;;;;;1;matrix or parameter set;Input;parameters;Parameter sets
for;Procedure;3;3;1
;;;;;1;init statement;Code piece;statements;Code piece or :literal, separated with semicolon
;;;;;2;condition expression;Code piece;expression;Code piece or :literal
;;;;;3;loop statement;Code piece;statements;Code piece or :literal, separated with semicolon
for all current table columns;Procedure;1;2;1
;;;;;1;iterator value;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;iterator counter;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
for all current table selected columns;Procedure;2;4;1
;;;;;1;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;2;iterator column number;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;3;iterator header name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;4;iterator counter;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
for all parameters;Procedure;2;3;1
;;;;;1;parameters;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;iterator value;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;3;iterator counter;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
for all table columns;Procedure;3;4;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;reference row number;Input;numeral;Numeral
;;;;;3;iterator value;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;4;iterator counter;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
for all table rows;Procedure;3;4;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;reference column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;iterator value;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;4;iterator counter;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
for all table selected columns;Procedure;4;6;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;reference row number;Input;numeral;Numeral
;;;;;4;iterator column number;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;5;iterator header name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;6;iterator counter;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
for all table selected rows;Procedure;4;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;reference column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;boolean expression to select rows;Code piece;expression;Code piece or :literal
;;;;;4;iterator value;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;5;iterator counter;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
for all variables;Procedure;2;4;1
;;;;;1;base variable name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;member variable value;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;3;iterator counter;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;4;member variable name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
for all variables if existing;Procedure;2;4;1
;;;;;1;base variable name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;member variable value;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;3;iterator counter;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;4;member variable name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
forget memorized table columns;Procedure;0;0;1
function existing;Function;1;1;1
;;;;;1;name of existing user function;Input;literal;Literal
geometric mean;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
geometric mean if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
geometric mean if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
geometric mean ignore blanks;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
geometric mean ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
geometric mean ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
geometric mean ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
geometric mean ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
geometric mean ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
get differences;Both;2;5;1
;;;;;1;1st string;Input;literal;Literal
;;;;;2;2nd string;Input;literal;Literal
;;;;;3;deviating part of 1st strin	g;Output;all;Value of any type
;;;;;4;deviating part of 2nd string;Output;all;Value of any type
;;;;;5;option;Input;literal;Literal
getch;Both;0;1;1
;;;;;1;character choices;Input;literal;Literal
getch ignore case;Both;0;1;1
;;;;;1;character choices;Input;literal;Literal
gini;Function;1;2;1
;;;;;1;input values;Input;parameters;Parameter sets
;;;;;2;lorenz curve;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
gini ignore blanks;Function;1;2;1
;;;;;1;input values;Input;parameters;Parameter sets
;;;;;2;lorenz curve;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
gini ignore zero;Function;1;2;1
;;;;;1;input values;Input;parameters;Parameter sets
;;;;;2;lorenz curve;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
global;Procedure;0;0;1
global privileges;Function;0;0;1
harmonic mean;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
harmonic mean if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
harmonic mean if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
hash signature;Function;1;99999999;1
;;;;;1;values;Input;to literal;Valid value of any type which is converted to literal
help;Procedure;0;0;1
horizontal;Function;1;1;1
;;;;;1;matrix or parameter set;Input;parameters;Parameter sets
hour;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
identify;Function;1;1;1
;;;;;1;variable name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
if;Procedure;1;1;1
;;;;;1;boolean expression;Input;boolean;Boolean type
include;Both;1;99999999;1
;;;;;1;program name;Input;literal;Literal
;;;;;2;parameter;Input;all;Value of any type
input;Function;0;3;1
;;;;;1;prompt;Input;literal;Literal
;;;;;2;checked type;Input;literal;Literal
;;;;;3;validation expression;Code piece;comparison expression;Code piece or :literal
input quick;Both;1;2;1
;;;;;1;prompt;Input;literal;Literal
;;;;;2;character choices;Input;literal;Literal
input quick ignore case;Both;1;2;1
;;;;;1;prompt;Input;literal;Literal
;;;;;2;character choices;Input;literal;Literal
inspect;Procedure;1;99999999;1
;;;;;1;destination variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
interactive;Procedure;0;0;1
irr;Function;1;2;1
;;;;;1;values to discount;Input;parameters;Parameter sets
;;;;;2;WACC;Input;literal;Literal
is integer;Function;1;1;1
;;;;;1;value;Input;all;Value of any type
is integer or blank;Function;1;1;1
;;;;;1;value;Input;all;Value of any type
is numeric;Function;1;1;1
;;;;;1;value;Input;all;Value of any type
is numeric or blank;Function;1;1;1
;;;;;1;value;Input;all;Value of any type
is strictly integer;Function;1;1;1
;;;;;1;value;Input;all;Value of any type
is strictly integer or blank;Function;1;1;1
;;;;;1;value;Input;all;Value of any type
is strictly numeric;Function;1;1;1
;;;;;1;value;Input;all;Value of any type
is strictly numeric or blank;Function;1;1;1
;;;;;1;value;Input;all;Value of any type
join;Function;1;4;1
;;;;;1;parameter set containing values;Input;parameters;Parameter sets
;;;;;2;opening or separator string;Input;literal;Literal
;;;;;3;separator string;Input;literal;Literal
;;;;;4;closing string;Input;literal;Literal
join if;Function;4;99999999;2
;;;;;1;values to join;Input;parameters;Parameter sets
;;;;;2;corresponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;4;values to compare or finally separation symbols;Input;parameters;Parameter sets
join if any;Function;4;99999999;2
;;;;;1;values to join;Input;parameters;Parameter sets
;;;;;2;corresponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;4;values to compare or finally separation symbols;Input;parameters;Parameter sets
join ignore blanks;Function;1;4;1
;;;;;1;parameter set containing values;Input;parameters;Parameter sets
;;;;;2;opening or separator string;Input;literal;Literal
;;;;;3;separator string;Input;literal;Literal
;;;;;4;closing string;Input;literal;Literal
join ignore blanks if;Function;4;99999999;2
;;;;;1;values to join;Input;parameters;Parameter sets
;;;;;2;corresponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;4;values to compare or finally separation symbols;Input;parameters;Parameter sets
join ignore blanks if any;Function;4;99999999;2
;;;;;1;values to join;Input;parameters;Parameter sets
;;;;;2;corresponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;4;values to compare or finally separation symbols;Input;parameters;Parameter sets
join junique if any;Function;4;99999999;2
;;;;;1;values to join;Input;parameters;Parameter sets
;;;;;2;corresponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;4;values to compare or finally separation symbols;Input;parameters;Parameter sets
join unique;Function;1;4;1
;;;;;1;parameter set containing values;Input;parameters;Parameter sets
;;;;;2;opening or separator string;Input;literal;Literal
;;;;;3;separator string;Input;literal;Literal
;;;;;4;closing string;Input;literal;Literal
join unique if;Function;4;99999999;2
;;;;;1;values to join;Input;parameters;Parameter sets
;;;;;2;corresponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;4;values to compare or finally separation symbols;Input;parameters;Parameter sets
join unique ignore blanks;Function;1;4;1
;;;;;1;parameter set containing values;Input;parameters;Parameter sets
;;;;;2;opening or separator string;Input;literal;Literal
;;;;;3;separator string;Input;literal;Literal
;;;;;4;closing string;Input;literal;Literal
join unique ignore blanks if;Function;4;99999999;2
;;;;;1;values to join;Input;parameters;Parameter sets
;;;;;2;corresponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;4;values to compare or finally separation symbols;Input;parameters;Parameter sets
join unique ignore blanks if any;Function;4;99999999;2
;;;;;1;values to join;Input;parameters;Parameter sets
;;;;;2;corresponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;4;values to compare or finally separation symbols;Input;parameters;Parameter sets
json to variable;Procedure;2;3;1
;;;;;1;name of variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;json text;Input;literal;Literal
;;;;;3;format descriptor;Input;literal;Literal
left;Both;2;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;follower;I/O;parameters;Parameter sets
left include;Both;2;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;follower;I/O;parameters;Parameter sets
left last match;Both;2;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;follower;I/O;parameters;Parameter sets
left last match include;Both;2;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;follower;I/O;parameters;Parameter sets
length;Function;1;1;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
license apply key;Both;3;3;1
;;;;;1;key;Input;literal;Literal
;;;;;2;holder;Input;literal;Literal
;;;;;3;machine or not user;Input;boolean;Boolean type
license provide identification;Function;0;0;1
linear;Function;2;2;1
;;;;;1;square matrix a;Input;parameters;Parameter sets
;;;;;2;vector b;Input;parameters;Parameter sets
linear interpolation;Function;3;5;1
;;;;;1;x value;Input;numerals;Numeral or parameter set containing numerals
;;;;;2;x coordinates;Input;parameters;Parameter sets
;;;;;3;y coordinates;Input;parameters;Parameter sets
;;;;;4;extrapolation options beyond left side;Input;numeral or literal;Literal version of numeral created
;;;;;5;extrapolation options beyond right side;Input;numeral or literal;Literal version of numeral created
list;Procedure;1;3;1
;;;;;1;table;Input;literal;Literal
;;;;;2;screen width;Input;numeral;Numeral
;;;;;3;page height;Input;numeral;Numeral
list functions;Procedure;0;1;1
;;;;;1;function name or wildcard;Input;literal;Literal
list global variables;Procedure;0;1;1
;;;;;1;variable name or wildcard;Input;literal;Literal
list local variables;Procedure;0;1;1
;;;;;1;variable name or wildcard;Input;literal;Literal
list system variables;Procedure;0;1;1
;;;;;1;variable name or wildcard;Input;literal;Literal
list variables;Procedure;0;1;1
;;;;;1;variable name or wildcard;Input;literal;Literal
literal;Function;1;4;1
;;;;;1;value to convert;Input;all;Value of any type
;;;;;2;formatting template;Input;literal or parameters;Literals or parameter sets
;;;;;3;formatting options or locale;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;placeholder symbol;Input;literal;Literal
ln;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
locate differences;Both;2;6;1
;;;;;1;1st string;Input;literal;Literal
;;;;;2;2nd string;Input;literal;Literal
;;;;;3;starting character position;Output;all;Value of any type
;;;;;4;length of deviation in 1st string;Output;all;Value of any type
;;;;;5;length of deviation in 2nd string;Output;all;Value of any type
;;;;;6;option;Input;literal;Literal
log;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
max;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max 123;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max 123 if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max 123 if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max 123 ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max 123 ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max 123 ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max abc;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max abc if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max abc if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max abc ignore case;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max abc ignore case if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max abc ignore case if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max ignore blanks;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max literal;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max literal if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max literal if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max literal ignore case;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max literal ignore case if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max literal ignore case if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max numeral;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max numeral if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max numeral if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max numeral ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
max numeral ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
max numeral ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
mdet;Function;1;1;1
;;;;;1;square matrix;Input;parameters;Parameter sets
mdiv;Function;2;2;1
;;;;;1;left matrix;Input;parameters;Parameter sets
;;;;;2;right matrix;Input;parameters;Parameter sets
median;Function;1;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
median if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
median if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
median ignore zero;Function;1;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
median ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
median ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
member count;Function;1;1;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
middle;Both;3;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;starting position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;length or matching pattern or parameter;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;4;follower;I/O;parameters;Parameter sets
middle include;Both;3;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;starting position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;length or matching pattern or parameter;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;4;follower;I/O;parameters;Parameter sets
middle until;Both;3;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;starting position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;length or matching pattern or parameter;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;4;follower;I/O;parameters;Parameter sets
middle until include;Both;3;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;starting position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;length or matching pattern or parameter;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;4;follower;I/O;parameters;Parameter sets
min;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min 123;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min 123 if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min 123 if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min 123 ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min 123 ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min 123 ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min abc;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min abc if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min abc if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min abc ignore blanks;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min abc ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min abc ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min abc ignore both;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min abc ignore both if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min abc ignore both if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min abc ignore case;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min abc ignore case if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min abc ignore case if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min ignore blanks;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min literal;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min literal if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min literal if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min literal ignore blanks;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min literal ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min literal ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min literal ignore both;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min literal ignore both if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min literal ignore both if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min literal ignore case;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min literal ignore case if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min literal ignore case if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min numeral;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min numeral if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min numeral if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min numeral ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
min numeral ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
min numeral ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
minute;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
minv;Function;1;1;1
;;;;;1;square matrix;Input;parameters;Parameter sets
mmul;Function;2;2;1
;;;;;1;left matrix;Input;parameters;Parameter sets
;;;;;2;right matrix;Input;parameters;Parameter sets
mod;Function;2;2;1
;;;;;1;divisor;Input;numeral;Numeral
;;;;;2;dividend;Input;numeral;Numeral
month;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
mul;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
name;Function;1;2;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;option;Input;literal;Literal
nearest neighbor interpolation;Function;3;5;1
;;;;;1;x value;Input;numerals;Numeral or parameter set containing numerals
;;;;;2;x coordinates;Input;parameters;Parameter sets
;;;;;3;y coordinates;Input;parameters;Parameter sets
;;;;;4;extrapolation options beyond left side;Input;numeral or literal;Literal version of numeral created
;;;;;5;extrapolation options beyond right side;Input;numeral or literal;Literal version of numeral created
nearest right neighbor interpolation;Function;3;5;1
;;;;;1;x value;Input;numerals;Numeral or parameter set containing numerals
;;;;;2;x coordinates;Input;parameters;Parameter sets
;;;;;3;y coordinates;Input;parameters;Parameter sets
;;;;;4;extrapolation options beyond left side;Input;numeral or literal;Literal version of numeral created
;;;;;5;extrapolation options beyond right side;Input;numeral or literal;Literal version of numeral created
null;Both;0;99999999;1
;;;;;1;parameter;Input;all;Value of any type
numeral;Function;1;1;1
;;;;;1;value;Input;valid;Numeral, Literal, Boolean, Date, Parameter
odd;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
office document properties;Procedure;3;3;1
;;;;;1;office file name;Input;literal;Literal
;;;;;2;property names;Output;parameters;Parameter sets
;;;;;3;property values;Output;parameters;Parameter sets
once;Procedure;0;0;1
or;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
outside;Both;3;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;starting position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;length or matching pattern or parameter;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;4;follower;I/O;parameters;Parameter sets
outside exclude;Both;3;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;starting position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;length or matching pattern or parameter;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;4;follower;I/O;parameters;Parameter sets
outside until;Both;3;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;starting position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;length or matching pattern or parameter;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;4;follower;I/O;parameters;Parameter sets
outside until exclude;Both;3;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;starting position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;length or matching pattern or parameter;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;4;follower;I/O;parameters;Parameter sets
parallel;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
parallel if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
parallel if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
parameter set;Function;1;1;1
;;;;;1;value;Input;all;Value of any type
pause;Procedure;0;99999999;1
;;;;;1;contents to display;Input;valid;Numeral, Literal, Boolean, Date, Parameter
pick;Function;2;99999999;1
;;;;;1;number as selector;Input;numeral;Numeral
;;;;;2;selected expression;Code piece;expression;Code piece or :literal
pick by value;Function;1;99999999;1
;;;;;1;selector value;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;expressions to match;Code piece;expression;Code piece or :literal
;;;;;3;selected expression;Code piece;expression;Code piece or :literal
pick if;Function;3;3;1
;;;;;1;boolean value as selector;Input;boolean;Boolean type
;;;;;2;selected expression if true;Code piece;expression;Code piece or :literal
;;;;;3;selected expression if false;Code piece;expression;Code piece or :literal
pick ifs;Function;3;99999999;2
;;;;;1;default value;Code piece;expression;Code piece or :literal
;;;;;2;condition expression;Code piece;expression;Code piece or :literal
;;;;;3;selected expression if true;Code piece;expression;Code piece or :literal
polynomial interpolation;Function;3;5;1
;;;;;1;x value;Input;numerals;Numeral or parameter set containing numerals
;;;;;2;x coordinates;Input;parameters;Parameter sets
;;;;;3;y coordinates;Input;parameters;Parameter sets
;;;;;4;extrapolation options beyond left side;Input;numeral or literal;Literal version of numeral created
;;;;;5;extrapolation options beyond right side;Input;numeral or literal;Literal version of numeral created
pow;Function;2;2;1
;;;;;1;base value;Input;numeral;Numeral
;;;;;2;exponent;Input;numeral;Numeral
prepare HTML for DataTables;Procedure;1;1;1
;;;;;1;file name;Input;literal;Literal
print;Both;0;99999999;1
;;;;;1;values;Input;to literal;Valid value of any type which is converted to literal
print line;Both;0;99999999;1
;;;;;1;values;Input;to literal;Valid value of any type which is converted to literal
product;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
product if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
product if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
product ignore blanks;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
product ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
product ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
product ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
product ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
product ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
prompt;Both;0;1;1
;;;;;1;prompt text;Input;literal;Literal
protect;Both;2;99999999;1
;;;;;1;protection setting;Input;literal;Literal
;;;;;2;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
protect recursive;Both;2;99999999;1
;;;;;1;protection setting;Input;literal;Literal
;;;;;2;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
pure date;Function;1;6;1
;;;;;1;date/time or year;Input;numeral or literal or date;Numerals, literals or dates
;;;;;2;month or date format;Input;numeral;Numeral
;;;;;3;day;Input;numeral;Numeral
;;;;;4;hour;Input;numeral;Numeral
;;;;;5;minute;Input;numeral;Numeral
;;;;;6;second;Input;numeral;Numeral
pure time;Function;1;3;1
;;;;;1;date with time or hour;Input;numeral or literal or date;Numerals, literals or dates
;;;;;2;minute;Input;numeral;Numeral
;;;;;3;second;Input;numeral;Numeral
quarter;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
quote path;Function;1;1;1
;;;;;1;path name;Input;literal;Literal
random;Function;1;2;1
;;;;;1;1st boundary value;Input;numeral;Numeral
;;;;;2;2nd boundary value;Input;numeral;Numeral
random letters;Function;1;1;1
;;;;;1;number of characters;Input;numeral;Numeral
random string;Function;1;1;1
;;;;;1;number of characters;Input;numeral;Numeral
read accessible;Function;1;1;1
;;;;;1;file name;Input;literal;Literal
read accessible repeat if locked;Function;1;1;1
;;;;;1;file name;Input;literal;Literal
redim;Procedure;3;99999999;3
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;dimensions;Input;numeral or literal or date literal;Literal Version of Numeral/Date created
;;;;;3;initial value;Input;all;Value of any type
redim protect;Procedure;4;99999999;3
;;;;;1;protection setting;Input;literal;Literal
;;;;;2;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;3;dimensions;Input;numeral or literal or date literal;Literal Version of Numeral/Date created
;;;;;4;initial value;Input;all;Value of any type
regression;Function;1;2;1
;;;;;1;values;Input;parameters;Parameter sets
;;;;;2;regression calculation method;Input;literal;Literal
release;Procedure;1;99999999;1
;;;;;1;reference to variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
release all;Both;0;99999999;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
replace;Function;3;4;1
;;;;;1;input literal;Input;literal;Literal
;;;;;2;existing contents;Input;literal or parameters;Literals or parameter sets
;;;;;3;new contents;Input;literal or parameters;Literals or parameter sets
;;;;;4;starting position;Input;numeral;Numeral
replace all;Function;3;4;1
;;;;;1;input literal;Input;literal;Literal
;;;;;2;existing contents;Input;literal or parameters;Literals or parameter sets
;;;;;3;new contents;Input;literal or parameters;Literals or parameter sets
;;;;;4;starting position;Input;numeral;Numeral
reschedule;Function;3;99999999;2
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
;;;;;2;rescheduling parameter 1;Input;numeral or literal;Literal version of numeral created
;;;;;3;rescheduling parameter 2;Input;numeral or literal or date;Numerals, literals or dates
resolve directory name;Function;1;1;1
;;;;;1;path name;Input;literal;Literal
resolve file name;Function;1;1;1
;;;;;1;path name;Input;literal;Literal
resolve path name;Function;1;1;1
;;;;;1;path name;Input;literal;Literal
return;Procedure;0;1;1
;;;;;1;return value;Input;valid;Numeral, Literal, Boolean, Date, Parameter
right;Both;2;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;follower;I/O;parameters;Parameter sets
right include;Both;2;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;position or matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;follower;I/O;parameters;Parameter sets
right last match;Both;2;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;follower;I/O;parameters;Parameter sets
right last match include;Both;2;99999999;1
;;;;;1;input value or parameter set;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;matching pattern;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;follower;I/O;parameters;Parameter sets
rms;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
rms if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
rms if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
rms ignore blanks;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
rms ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
rms ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
rms ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
rms ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
rms ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
round;Function;1;2;1
;;;;;1;number to round;Input;numeral;Numeral
;;;;;2;rounding interval;Input;numeral;Numeral
round down;Function;1;2;1
;;;;;1;number to round;Input;numeral;Numeral
;;;;;2;rounding interval;Input;numeral;Numeral
round up;Function;1;2;1
;;;;;1;number to round;Input;numeral;Numeral
;;;;;2;rounding interval;Input;numeral;Numeral
row;Function;0;1;1
;;;;;1;depth;Input;numeral;Numeral
scope;Function;1;1;1
;;;;;1;variable name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
script privileges;Function;0;0;1
search files;Function;0;4;1
;;;;;1;path name;Input;literal;Literal
;;;;;2;match name;Input;literal;Literal
;;;;;3;match count limitation;Input;numeral;Numeral
;;;;;4;search options;Input;literal;Literal
search files recursive;Function;0;4;1
;;;;;1;path name;Input;literal;Literal
;;;;;2;match name;Input;literal;Literal
;;;;;3;match count limitation;Input;numeral;Numeral
;;;;;4;search options;Input;literal;Literal
second;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
select;Function;2;99999999;1
;;;;;1;number as selector;Input;numeral;Numeral
;;;;;2;selected value;Input;all;Value of any type
select by value;Function;1;99999999;1
;;;;;1;selector value;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;2;values to match;Input;valid;Numeral, Literal, Boolean, Date, Parameter
;;;;;3;selected value;Input;all;Value of any type
select if;Function;3;3;1
;;;;;1;boolean value as selector;Input;boolean;Boolean type
;;;;;2;selected value if true;Input;all;Value of any type
;;;;;3;selected value if false;Input;all;Value of any type
select ifs;Function;3;99999999;2
;;;;;1;default value;Input;all;Value of any type
;;;;;2;condition;Input;boolean;Boolean type
;;;;;3;value if true;Input;all;Value of any type
serial date;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
set;Function;1;3;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;level;Input;numeral;Numeral
;;;;;3;all;Input;literal;Literal
set locale;Procedure;1;2;1
;;;;;1;locale or language or country;Input;literal;Literal
;;;;;2;file name to list of locales;Input;literal;Literal
set names;Function;1;3;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;level;Input;numeral;Numeral
;;;;;3;all;Input;literal;Literal
sin;Function;1;1;1
;;;;;1;angle in radians;Input;numeral;Numeral
sin deg;Function;1;1;1
;;;;;1;angle in degrees;Input;numeral;Numeral
sinh;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
sleep;Procedure;1;2;1
;;;;;1;time in seconds;Input;numeral;Numeral
;;;;;2;prompt;Input;literal;Literal
sleep countdown;Both;1;2;1
;;;;;1;time in seconds;Input;numeral;Numeral
;;;;;2;prompt;Input;literal;Literal
sleep until;Procedure;1;2;1
;;;;;1;target date / time;Input;to date;Values which are dates or can be converted to dates
;;;;;2;prompt;Input;literal;Literal
sleep until countdown;Both;1;2;1
;;;;;1;target date / time;Input;to date;Values which are dates or can be converted to dates
;;;;;2;prompt;Input;literal;Literal
smart if numeral;Function;1;4;1
;;;;;1;value;Input;numeral or literal;Literal version of numeral created
;;;;;2;decimal symbol;Input;literal;Literal
;;;;;3;custom units;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;custom scaling factors;Input;numerals;Numeral or parameter set containing numerals
smart numeral;Function;1;4;1
;;;;;1;value;Input;numeral or literal;Literal version of numeral created
;;;;;2;decimal symbol;Input;literal;Literal
;;;;;3;custom units;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;custom scaling factors;Input;numerals;Numeral or parameter set containing numerals
softquoted literal;Function;1;4;1
;;;;;1;value to convert;Input;all;Value of any type
;;;;;2;formatting template;Input;literal or parameters;Literals or parameter sets
;;;;;3;formatting options or locale;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;placeholder symbol;Input;literal;Literal
spline interpolation;Function;3;5;1
;;;;;1;x value;Input;numerals;Numeral or parameter set containing numerals
;;;;;2;x coordinates;Input;parameters;Parameter sets
;;;;;3;y coordinates;Input;parameters;Parameter sets
;;;;;4;extrapolation options beyond left side;Input;numeral or literal;Literal version of numeral created
;;;;;5;extrapolation options beyond right side;Input;numeral or literal;Literal version of numeral created
sqrt;Function;1;1;1
;;;;;1;positive number;Input;numeral;Numeral
start;Both;1;99999999;1
;;;;;1;program name;Input;literal;Literal
;;;;;2;parameter;Input;all;Value of any type
starting directory;Both;0;1;1
;;;;;1;directory path;Input;literal;Literal
statements;Procedure;1;1;1
;;;;;1;statements;Code piece;statements;Code piece or :literal, separated with semicolon
stop;Procedure;0;0;1
structure;Procedure;3;99999999;3
;;;;;1;variable name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;Member names;Input;parameters;Parameter sets
;;;;;3;initial values;Input;parameters;Parameter sets
structure protect;Procedure;4;99999999;3
;;;;;1;protection level;Input;literal;Literal
;;;;;2;variable name;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;3;Member names;Input;parameters;Parameter sets
;;;;;4;initial values;Input;parameters;Parameter sets
structure to array;Procedure;1;99999999;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
structure to array recursive;Procedure;1;99999999;1
;;;;;1;variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
sub;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
subset;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;subset to check;Input;to parameters;Valid types are converted to parameter sets
subset ignore blanks;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;subset to check;Input;to parameters;Valid types are converted to parameter sets
subset ignore both;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;subset to check;Input;to parameters;Valid types are converted to parameter sets
subset ignore case;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;subset to check;Input;to parameters;Valid types are converted to parameter sets
subset recursive;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;subset to check;Input;to parameters;Valid types are converted to parameter sets
subset recursive ignore blanks;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;subset to check;Input;to parameters;Valid types are converted to parameter sets
subset recursive ignore both;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;subset to check;Input;to parameters;Valid types are converted to parameter sets
subset recursive ignore case;Function;2;2;1
;;;;;1;input parameter set;Input;to parameters;Valid types are converted to parameter sets
;;;;;2;subset to check;Input;to parameters;Valid types are converted to parameter sets
substitute;Procedure;3;4;1
;;;;;1;input literal;I/O;literal;Literal
;;;;;2;existing contents;Input;literal or parameters;Literals or parameter sets
;;;;;3;new contents;Input;literal or parameters;Literals or parameter sets
;;;;;4;starting position;Input;numeral;Numeral
substitute all;Procedure;3;4;1
;;;;;1;input literal;I/O;literal;Literal
;;;;;2;existing contents;Input;literal or parameters;Literals or parameter sets
;;;;;3;new contents;Input;literal or parameters;Literals or parameter sets
;;;;;4;starting position;Input;numeral;Numeral
sum;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
sum if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
sum if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
switch;Procedure;1;1;1
;;;;;1;expression;Input;numeral or literal or date literal;Literal Version of Numeral/Date created
system;Both;0;99999999;1
;;;;;1;system command;Input;literal;Literal
table append;Both;2;99999999;1
;;;;;1;name of new table;Input;literal;Literal
;;;;;2;initial contents;Input;parameters;Parameter sets
table append blank rows;Procedure;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;number of rows to append;Input;numeral;Numeral
table append on same field;Both;2;2;1
;;;;;1;name of new table;Input;literal;Literal
;;;;;2;initial contents;Input;literal;Literal
table append on same row;Both;2;99999999;1
;;;;;1;name of new table;Input;literal;Literal
;;;;;2;initial contents;Input;parameters;Parameter sets
table arrange;Procedure;1;99999999;1
;;;;;1;name of existing destination table;Input;literal;Literal
table arrange with spacing;Procedure;1;99999999;1
;;;;;1;name of existing destination table;Input;literal;Literal
table check;Both;3;9;1
;;;;;1;name of target table;Input;literal;Literal
;;;;;2;name of reference table;Input;literal;Literal
;;;;;3;columns with data to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns with row match identifiers;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;columns with group identifiers;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;columns with group match identifiers;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;column w. addt'l comparison expressions;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;8;column with comparison directions;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;9;expression to select rows;Code piece;expression;Code piece or :literal
table check duplicates;Both;3;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;duplicate identifier column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;duplicate index column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;duplicate count column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table check duplicates ignore blanks;Both;3;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;duplicate identifier column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;duplicate index column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;duplicate count column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table check duplicates ignore blanks selected rows;Both;4;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;duplicate identifier column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;duplicate index column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;duplicate count column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table check duplicates ignore both;Both;3;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;duplicate identifier column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;duplicate index column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;duplicate count column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table check duplicates ignore both selected rows;Both;4;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;duplicate identifier column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;duplicate index column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;duplicate count column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table check duplicates ignore case;Both;3;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;duplicate identifier column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;duplicate index column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;duplicate count column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table check duplicates ignore case selected rows;Both;4;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;duplicate identifier column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;duplicate index column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;duplicate count column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table check duplicates selected rows;Both;4;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;duplicate identifier column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;duplicate index column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;duplicate count column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table check header;Function;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;column to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;row number;Input;numeral;Numeral
table check headers;Both;2;99999999;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;check header option 1;Input;literal;Literal
;;;;;4;additional option(s);Input;literal;Literal
table check headers silently;Both;2;99999999;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;check header option 1;Input;literal;Literal
;;;;;4;additional option(s);Input;literal;Literal
table check row;Function;2;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;row number;Input;numeral;Numeral
;;;;;3;criterion;Input;literal;Literal
;;;;;4;columns to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table clear;Procedure;1;99999999;1
;;;;;1;name of existing table;Input;literal;Literal
table column number;Function;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;column to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;row number;Input;numeral;Numeral
table compare;Both;3;7;1
;;;;;1;name of existing newer table;Input;literal;Literal
;;;;;2;name of existing older table;Input;literal;Literal
;;;;;3;name of report table;Input;literal;Literal
;;;;;4;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;reporting option;Input;literal;Literal
;;;;;6;change indication function;Input;literal;Literal
;;;;;7;call function only if different;Input;boolean;Boolean type
table compare ignore blanks;Both;3;7;1
;;;;;1;name of existing newer table;Input;literal;Literal
;;;;;2;name of existing older table;Input;literal;Literal
;;;;;3;name of report table;Input;literal;Literal
;;;;;4;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;reporting option;Input;literal;Literal
;;;;;6;change indication function;Input;literal;Literal
;;;;;7;call function only if different;Input;boolean;Boolean type
table compare ignore both;Both;3;7;1
;;;;;1;name of existing newer table;Input;literal;Literal
;;;;;2;name of existing older table;Input;literal;Literal
;;;;;3;name of report table;Input;literal;Literal
;;;;;4;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;reporting option;Input;literal;Literal
;;;;;6;change indication function;Input;literal;Literal
;;;;;7;call function only if different;Input;boolean;Boolean type
table compare ignore case;Both;3;7;1
;;;;;1;name of existing newer table;Input;literal;Literal
;;;;;2;name of existing older table;Input;literal;Literal
;;;;;3;name of report table;Input;literal;Literal
;;;;;4;orientation columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;reporting option;Input;literal;Literal
;;;;;6;change indication function;Input;literal;Literal
;;;;;7;call function only if different;Input;boolean;Boolean type
table configure;Procedure;3;99999999;2
;;;;;1;names of existing tables;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;configuration parameter name;Input;literal;Literal
;;;;;3;configuration parameter value;Input;literal;Literal
table consolidate;Both;1;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;identifier columns to match;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;5;separator string;Input;literal;Literal
table consolidate selected rows;Both;2;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;identifier columns to match;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;separator string;Input;literal;Literal
table copy columns;Procedure;3;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;existing columns to copy;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;new column headers;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table copy columns selected rows;Procedure;4;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;existing columns to copy;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;new column headers;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table copy table;Procedure;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;name of new table;Input;literal;Literal
table copy table columns;Procedure;3;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;name of new table;Input;literal;Literal
;;;;;3;columns to copy;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table copy table columns selected rows;Procedure;4;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;name of new table;Input;literal;Literal
;;;;;3;columns to copy;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;expression to select rows to copy;Code piece;expression;Code piece or :literal
table copy table selected rows;Procedure;3;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;name of new table;Input;literal;Literal
;;;;;3;expression to select rows;Code piece;expression;Code piece or :literal
table correct headers;Both;3;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;existing columns to rename;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;new column header names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
table correct headers ignore case;Both;3;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;existing columns to rename;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;new column header names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
table create;Both;1;99999999;1
;;;;;1;name of new table;Input;literal;Literal
table create if not existing;Both;1;99999999;1
;;;;;1;name of new table;Input;literal;Literal
table delete;Procedure;1;99999999;1
;;;;;1;name of existing table;Input;literal;Literal
table delete all blank columns;Procedure;1;99999999;1
;;;;;1;name of existing tables;Input;literal;Literal
table delete all empty columns;Procedure;1;99999999;1
;;;;;1;name of existing tables;Input;literal;Literal
table delete all unnamed columns;Procedure;1;99999999;1
;;;;;1;name of existing tables;Input;literal;Literal
table delete blank columns;Procedure;1;99999999;1
;;;;;1;name of existing tables;Input;literal;Literal
table delete blank rows;Both;1;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;number of consecutive blank rows to keep;Input;numeral;Numeral
;;;;;3;criterion;Input;literal;Literal
;;;;;4;columns to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table delete cells selected columns;Both;3;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns selected;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;row number;Input;numeral;Numeral
;;;;;4;count;Input;numeral;Numeral
table delete cells selected rows;Both;3;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;count;Input;numeral;Numeral
table delete columns;Both;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to delete;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;number of columns to delete;Input;numeral;Numeral
table delete empty columns;Procedure;1;99999999;1
;;;;;1;name of existing tables;Input;literal;Literal
table delete remaining columns;Both;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;starting column delete;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table delete remaining rows;Procedure;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;row number;Input;numeral;Numeral
table delete rows;Procedure;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;row number;Input;numeral;Numeral
;;;;;3;number of rows to delete;Input;numeral;Numeral
table delete selected rows;Both;2;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;number of rows to delete or keep;Input;numeral;Numeral
;;;;;4;offset;Input;numeral;Numeral
table delete silently;Procedure;1;99999999;1
;;;;;1;name of existing table;Input;literal;Literal
table delete unnamed columns;Procedure;1;99999999;1
;;;;;1;name of existing tables;Input;literal;Literal
table distribute;Both;9;15;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;total values to distribute;Input;numeral or literal;Literal version of numeral created
;;;;;3;batch sizes;Input;numeral or literal;Literal version of numeral created
;;;;;4;start dates;Input;date or literal;Dates or literals
;;;;;5;end dates or daily capacity;Input;numeral or literal or date;Numerals, literals or dates
;;;;;6;intervals;Input;literal;Literal
;;;;;7;usance;Input;literal;Literal
;;;;;8;output start date;Input;to date;Values which are dates or can be converted to dates
;;;;;9;output end date;Input;to date;Values which are dates or can be converted to dates
;;;;;10;header date format;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;11;distributed values;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;12;earlier sums;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;13;later sums;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;14;bank holidays;Input;parameters;Parameter sets
;;;;;15;school holidays;Input;parameters;Parameter sets
table distribute accumulate;Both;9;15;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;total values to distribute;Input;numeral or literal;Literal version of numeral created
;;;;;3;batch sizes;Input;numeral or literal;Literal version of numeral created
;;;;;4;start dates;Input;date or literal;Dates or literals
;;;;;5;end dates or daily capacity;Input;numeral or literal or date;Numerals, literals or dates
;;;;;6;intervals;Input;literal;Literal
;;;;;7;usance;Input;literal;Literal
;;;;;8;output start date;Input;to date;Values which are dates or can be converted to dates
;;;;;9;output end date;Input;to date;Values which are dates or can be converted to dates
;;;;;10;header date format;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;11;distributed values;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;12;earlier sums;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;13;later sums;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;14;bank holidays;Input;parameters;Parameter sets
;;;;;15;school holidays;Input;parameters;Parameter sets
table distribute advance;Both;9;15;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;total values to distribute;Input;numeral or literal;Literal version of numeral created
;;;;;3;batch sizes;Input;numeral or literal;Literal version of numeral created
;;;;;4;start dates;Input;date or literal;Dates or literals
;;;;;5;end dates or daily capacity;Input;numeral or literal or date;Numerals, literals or dates
;;;;;6;intervals;Input;literal;Literal
;;;;;7;usance;Input;literal;Literal
;;;;;8;output start date;Input;to date;Values which are dates or can be converted to dates
;;;;;9;output end date;Input;to date;Values which are dates or can be converted to dates
;;;;;10;header date format;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;11;distributed values;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;12;earlier sums;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;13;later sums;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;14;bank holidays;Input;parameters;Parameter sets
;;;;;15;school holidays;Input;parameters;Parameter sets
table distribute advance accumulate;Both;9;15;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;total values to distribute;Input;numeral or literal;Literal version of numeral created
;;;;;3;batch sizes;Input;numeral or literal;Literal version of numeral created
;;;;;4;start dates;Input;date or literal;Dates or literals
;;;;;5;end dates or daily capacity;Input;numeral or literal or date;Numerals, literals or dates
;;;;;6;intervals;Input;literal;Literal
;;;;;7;usance;Input;literal;Literal
;;;;;8;output start date;Input;to date;Values which are dates or can be converted to dates
;;;;;9;output end date;Input;to date;Values which are dates or can be converted to dates
;;;;;10;header date format;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;11;distributed values;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;12;earlier sums;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;13;later sums;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;14;bank holidays;Input;parameters;Parameter sets
;;;;;15;school holidays;Input;parameters;Parameter sets
table divide;Procedure;3;5;1
;;;;;1;name of existing numerator table;Input;literal;Literal
;;;;;2;name of existing denominator table;Input;literal;Literal
;;;;;3;name of quotient table;Input;literal;Literal
;;;;;4;numerator columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;categorization columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table divide selected rows;Procedure;4;6;1
;;;;;1;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;2;name of existing numerator table;Input;literal;Literal
;;;;;3;name of existing denominator table;Input;literal;Literal
;;;;;4;name of quotient table;Input;literal;Literal
;;;;;5;numerator columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;categorization columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table duplicate columns;Both;3;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to duplicate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;new header names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;destination column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table exclude;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table exclude exclusive columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table exclude extend columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table exclude intersect columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table exclude subtract columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table existing;Both;1;1;1
;;;;;1;table name;Input;literal;Literal
table expand;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table expand fast;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table expand fast ignore case;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table expand fast smart;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table expand fast smart ignore case;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table expand ignore case;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table expand smart;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table expand smart ignore case;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table explore;Function;1;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;starting rows;Input;numeral;Numeral
;;;;;3;ending rows;Input;numeral;Numeral
table extract;Procedure;4;6;1
;;;;;1;destination table name;Input;literal;Literal
;;;;;2;source table name;Input;literal;Literal
;;;;;3;coordinates;Input;parameters;Parameter sets
;;;;;4;index;Input;numeral;Numeral
;;;;;5;row offset;Input;numeral;Numeral
;;;;;6;column offset;Input;numeral;Numeral
table extract fill;Procedure;4;6;1
;;;;;1;destination table name;Input;literal;Literal
;;;;;2;source table name;Input;literal;Literal
;;;;;3;coordinates;Input;parameters;Parameter sets
;;;;;4;index;Input;numeral;Numeral
;;;;;5;row offset;Input;numeral;Numeral
;;;;;6;column offset;Input;numeral;Numeral
table fill horizontally;Procedure;1;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;starting column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;ending column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;matching string;Input;literal;Literal
;;;;;5;direction;Input;literal;Literal
table fill horizontally selected rows;Procedure;2;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;starting column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;ending column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;matching string;Input;literal;Literal
;;;;;6;direction;Input;literal;Literal
table fill vertically;Procedure;2;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to repeat;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;matching strings;Input;literal or parameters;Literals or parameter sets
;;;;;4;direction;Input;literal;Literal
table fill vertically selected rows;Procedure;3;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;columns to repeat;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;matching strings;Input;literal or parameters;Literals or parameter sets
;;;;;5;direction;Input;literal;Literal
table filter;Function;2;8;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;coordinates;Input;parameters;Parameter sets
;;;;;3;header matching pattern;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;header rows;Input;numeral;Numeral
;;;;;5;inside area filter;Input;parameters;Parameter sets
;;;;;6;overlay area filter;Input;parameters;Parameter sets
;;;;;7;size filter;Input;parameters;Parameter sets
;;;;;8;size adjustment;Input;parameters;Parameter sets
table filter ignore case;Function;2;8;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;coordinates;Input;parameters;Parameter sets
;;;;;3;header matching pattern;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;header rows;Input;numeral;Numeral
;;;;;5;inside area filter;Input;parameters;Parameter sets
;;;;;6;overlay area filter;Input;parameters;Parameter sets
;;;;;7;size filter;Input;parameters;Parameter sets
;;;;;8;size adjustment;Input;parameters;Parameter sets
table fit;Both;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;option;Input;literal;Literal
;;;;;3;padding contents;Input;literal;Literal
table format numbers;Procedure;3;5;1
;;;;;1;table;Input;literal;Literal
;;;;;2;columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;formatting descriptor;Input;literal;Literal
;;;;;4;locale;Input;literal;Literal
;;;;;5;placeholder symbol;Input;literal;Literal
table initialize;Both;2;99999999;1
;;;;;1;name of new table;Input;literal;Literal
;;;;;2;initial contents;Input;parameters;Parameter sets
table insert above selected rows;Both;2;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;expression for row count;Code piece;expression;Code piece or :literal
;;;;;4;columns to copy downward;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;index column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table insert columns;Both;2;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;new header names;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;initial values;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;destination column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table insert missing columns;Both;2;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;new header names;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;initial values;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;destination column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table insert rows;Procedure;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;row number;Input;numeral;Numeral
;;;;;3;number of rows to insert;Input;numeral;Numeral
table insert selected rows;Both;2;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;expression for row count;Code piece;expression;Code piece or :literal
;;;;;4;columns to copy downward;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;index column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate fast;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate fast ignore case;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate ignore case;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate once;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate once ignore case;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate smart;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate smart ignore case;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate smart once;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate smart once ignore case;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate top down;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table integrate top down ignore case;Both;6;9;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;lookup table: value distribution options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;8;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;9;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table intersect;Both;2;6;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;separation symbol;Input;literal;Literal
table intersect exclusive columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table intersect extend columns;Both;2;6;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;separation symbol;Input;literal;Literal
table intersect intersect columns;Both;2;6;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;separation symbol;Input;literal;Literal
table intersect subtract columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table keep columns;Procedure;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to keep;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table keep existing columns;Procedure;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to keep;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table keep selected rows;Both;2;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;number of rows to delete or keep;Input;numeral;Numeral
;;;;;4;offset;Input;numeral;Numeral
table length;Function;1;1;1
;;;;;1;name of existing table;Input;literal;Literal
table lift contents;Both;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;check header option 1;Input;literal;Literal
table lift header row;Both;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;check header option 1;Input;literal;Literal
table lift headers;Both;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;check header option 1;Input;literal;Literal
table list;Procedure;1;99999999;2
;;;;;1;table name;Input;literal;Literal
;;;;;2;option;Input;literal;Literal
;;;;;3;value;Input;numeral;Numeral
table load;Both;2;4;1
;;;;;1;name of table;Input;literal;Literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;file format descriptor;Input;literal;Literal
;;;;;4;additional info;Input;literal or parameters;Literals or parameter sets
table load and keep;Procedure;3;3;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;columns to keep;Input;parameters;Parameter sets
table load csv and keep;Procedure;3;3;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;columns to keep;Input;parameters;Parameter sets
table load excel file;Procedure;2;3;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;excel file name;Input;literal;Literal
;;;;;3;excel sheet name;Input;numeral or literal;Literal version of numeral created
table load unchanged;Both;2;3;1
;;;;;1;name of table;Input;literal;Literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;file format descriptor;Input;literal;Literal
table lookup;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup fast;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup fast ignore case;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup ignore case;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup once;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup once ignore case;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup smart;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup smart ignore case;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup smart once;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup smart once ignore case;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup top down;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table lookup top down ignore case;Both;6;8;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;destination table: input columns (to match);Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;destination table: destination columns (received from lookup table;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;name of existing lookup table;Input;literal;Literal
;;;;;5;lookup table: search pattern columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;lookup table: output columns;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;8;lookup table: access statistics column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table manipulate;Procedure;4;4;1
;;;;;1;name of existing target table;Input;literal;Literal
;;;;;2;name of existing lookup table;Input;literal;Literal
;;;;;3;column with boolean expressions;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;column with statements;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table max width;Function;1;1;1
;;;;;1;name of existing table;Input;literal;Literal
table menu;Function;4;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;input choices column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;explanation column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;selection column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table menu quick;Function;4;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;input choices column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;explanation column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;selection column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table merge;Both;2;6;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;separation symbol;Input;literal;Literal
table merge exclusive columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table merge extend columns;Both;2;6;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;separation symbol;Input;literal;Literal
table merge intersect columns;Both;2;6;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;separation symbol;Input;literal;Literal
table merge subtract columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table min width;Function;1;1;1
;;;;;1;name of existing table;Input;literal;Literal
table multiply;Procedure;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;destination column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table multiply selected rows;Procedure;3;4;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;4;destination column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table overlay;Both;2;6;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;separation symbol;Input;literal;Literal
table overlay exclusive columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table overlay extend columns;Both;2;6;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;separation symbol;Input;literal;Literal
table overlay intersect columns;Both;2;6;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to consolidate;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;consolidation actions;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;separation symbol;Input;literal;Literal
table process;Both;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;statements to run;Code piece;statements;Code piece or :literal, separated with semicolon
table process selected rows;Both;3;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;statements to run if true;Code piece;statements;Code piece or :literal, separated with semicolon
;;;;;4;statements to run if false;Code piece;statements;Code piece or :literal, separated with semicolon
table process selected rows fast;Both;5;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;boolean expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;row number variable;I/O;numeral;Numeral
;;;;;4;processing option;Input;literal;Literal
;;;;;5;statements to apply;Code piece;statements;Code piece or :literal, separated with semicolon
table rank rows;Both;3;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;destination column for rank numbers;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;columns for ranking order;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;ranking options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;5;ranking scheme for equivalent values;Input;numeral;Numeral
;;;;;6;rank starting number;Input;numeral;Numeral
table rank selected rows;Both;4;7;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;destination column for rank numbers;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns for ranking order;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;ranking options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;ranking scheme for equivalent values;Input;numeral;Numeral
;;;;;7;rank starting number;Input;numeral;Numeral
table read column selected rows;Function;3;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;selected source column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;boolean expression;Code piece;expression;Code piece or :literal
table rearrange columns;Procedure;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to rearrange;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table rearrange existing columns;Procedure;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to rearrange;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table rearrange selected rows;Procedure;3;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;destination row specifier;Input;literal;Literal
table rename;Procedure;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;new table name;Input;literal;Literal
table rename column headers;Both;3;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;existing columns to rename;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;new column header names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
table rename existing headers;Both;3;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;existing columns to rename;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;new column header names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
table rename headers;Both;3;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;existing columns to rename;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;new column header names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
table row width;Function;0;2;1
;;;;;1;name of existing table;Input;numeral or literal;Literal version of numeral created
;;;;;2;row number;Input;numeral;Numeral
table save;Both;2;4;1
;;;;;1;name of existing tables;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;file format descriptor;Input;literal;Literal
;;;;;4;file format descriptor;Input;literal;Literal
table save multiple;Both;3;5;1
;;;;;1;name of existing tables;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;excel sheet names assigned;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;file name;Input;literal;Literal
;;;;;4;file format descriptor;Input;literal;Literal
;;;;;5;file format descriptor;Input;literal;Literal
table save with decimal comma;Both;2;4;1
;;;;;1;name of existing tables;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;file format descriptor;Input;literal;Literal
;;;;;4;file format descriptor;Input;literal;Literal
table save with local decimal separator;Both;2;4;1
;;;;;1;name of existing tables;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;2;file name;Input;literal;Literal
;;;;;3;file format descriptor;Input;literal;Literal
;;;;;4;file format descriptor;Input;literal;Literal
table search;Both;4;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;search pattern in comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;starting row number;Output;numeral;Numeral
;;;;;4;starting column number;Output;numeral;Numeral
;;;;;5;left boundary column number;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;right boundary column number;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table search header row;Both;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to check;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;check header option 1;Input;literal;Literal
table search row;Function;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;boolean expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;starting row number;Input;numeral;Numeral
table search vertically;Both;4;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;search pattern in comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;starting row number;Output;numeral;Numeral
;;;;;4;starting column number;Output;numeral;Numeral
;;;;;5;left boundary column number;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;right boundary column number;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table selected column numbers;Function;2;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;row number;Input;numeral;Numeral
;;;;;4;starting column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table selected columns;Function;2;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;row number;Input;numeral;Numeral
;;;;;4;starting column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table selected headers;Function;2;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;comparison expression;Code piece;comparison expression;Code piece or :literal
;;;;;3;row number;Input;numeral;Numeral
;;;;;4;starting column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table selected rows;Function;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
table serialize;Both;5;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to repeat;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;column for sequence names;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;sequence names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;5;category names;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;original header template;Input;literal;Literal
table serialize all;Both;5;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to repeat;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;column for sequence names;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;sequence names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;5;category names;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;original header template;Input;literal;Literal
table serialize ignore zero;Both;5;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to repeat;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;column for sequence names;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;sequence names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;5;category names;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;original header template;Input;literal;Literal
table sort columns;Procedure;1;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;column to mark begin of sorting range;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;column to mark end of sorting range;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;sorting option;Input;literal;Literal
;;;;;5;row number;Input;numeral;Numeral
table sort rows;Both;2;3;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns for sorting order;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;sorting options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
table sort selected rows;Both;3;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;columns for sorting order;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;sorting options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
table split table columns;Procedure;3;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;naem of new table;Input;literal;Literal
;;;;;3;columns to split;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;columns to copy;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table split table selected rows;Procedure;3;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;name of new table;Input;literal;Literal
;;;;;3;expression to select rows to split;Code piece;expression;Code piece or :literal
;;;;;4;expression to select rows to copy;Code piece;expression;Code piece or :literal
table spread;Both;3;7;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;column with values to spread;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;spreading key expression;Code piece;expression;Code piece or :literal
;;;;;4;pivot limitation expression;Code piece;expression;Code piece or :literal
;;;;;5;maximum number of columns to add;Input;numeral;Numeral
;;;;;6;column for 'others';Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;destination column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table spread accumulating;Both;3;7;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;column with values to spread;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;spreading key expression;Code piece;expression;Code piece or :literal
;;;;;4;pivot limitation expression;Code piece;expression;Code piece or :literal
;;;;;5;maximum number of columns to add;Input;numeral;Numeral
;;;;;6;column for 'others';Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;7;destination column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table spread given headers;Procedure;4;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;column with values to spread;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;spreading key expression;Code piece;expression;Code piece or :literal
;;;;;4;columns to be inserted;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;column for 'others';Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;desetination column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table spread given headers accumulating;Procedure;4;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;column with values to spread;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;spreading key expression;Code piece;expression;Code piece or :literal
;;;;;4;columns to be inserted;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;column for 'others';Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;6;desetination column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table substitute horizontally;Procedure;1;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;starting column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;ending column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;substitution string;Input;literal;Literal
;;;;;5;direction;Input;literal;Literal
table substitute horizontally selected rows;Procedure;2;6;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;starting column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;ending column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;5;substitution string;Input;literal;Literal
;;;;;6;direction;Input;literal;Literal
table substitute vertically;Procedure;2;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;columns to substitute;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;substitution strings;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;direction;Input;literal;Literal
table substitute vertically selected rows;Procedure;3;5;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;expression to select rows;Code piece;expression;Code piece or :literal
;;;;;3;columns to substitute;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;4;substitution strings;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;5;direction;Input;literal;Literal
table subtract;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table subtract columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table subtract exclusive columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table subtract extend columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table subtract intersect columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table subtract subtract columns;Both;2;3;1
;;;;;1;name of existing source table;Input;literal;Literal
;;;;;2;name of existing destination table;Input;literal;Literal
;;;;;3;identifier columns to match rows;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
table transpose;Procedure;1;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;name of new table;Input;literal;Literal
table write column selected rows;Both;4;4;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;selected destination column;Input;columns;Literal, numeral, or parameter set containing literals and/or numerals
;;;;;3;boolean expression;Code piece;expression;Code piece or :literal
;;;;;4;contents to write;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
tan;Function;1;1;1
;;;;;1;angle in radians;Input;numeral;Numeral
tan deg;Function;1;1;1
;;;;;1;angle in degrees;Input;numeral;Numeral
tanh;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
test;Both;0;0;1
text color;Both;0;1;1
;;;;;1;color;Input;literal;Literal
throw;Procedure;0;1;1
;;;;;1;return value or exit code;Input;valid;Numeral, Literal, Boolean, Date, Parameter
time;Function;1;3;1
;;;;;1;date with time or hour;Input;numeral or literal or date;Numerals, literals or dates
;;;;;2;minute;Input;numeral;Numeral
;;;;;3;second;Input;numeral;Numeral
time inside;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
tokenize;Function;1;7;1
;;;;;1;string;Input;literal;Literal
;;;;;2;token separators;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;quotation marks;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;4;additional tokens;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;5;comment symbols;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;6;line comment symbols;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;7;options;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
transpose;Function;1;1;1
;;;;;1;matrix;Input;parameters;Parameter sets
trim;Function;1;1;1
;;;;;1;input value or parameter set;Input;literal or parameters;Literals or parameter sets
trim all;Function;1;1;1
;;;;;1;input literal;Input;literal;Literal
tv;Function;3;3;1
;;;;;1;FCFn;Input;numeral;Numeral
;;;;;2;growth rate;Input;numeral;Numeral
;;;;;3;WACC;Input;numeral;Numeral
type;Function;1;1;1
;;;;;1;variable;Input;all;Value of any type
type detailed;Function;1;1;1
;;;;;1;variable;Input;all;Value of any type
unless;Procedure;1;1;1
;;;;;1;boolean expression;Input;boolean;Boolean type
until;Procedure;1;1;1
;;;;;1;boolean expression;Input;boolean;Boolean type
user function existing;Function;1;1;1
;;;;;1;name of existing user function;Input;literal;Literal
variable load;Procedure;2;3;1
;;;;;1;name of variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;file name;Input;literal;Literal
;;;;;3;format descriptor;Input;literal;Literal
variable save;Procedure;2;3;1
;;;;;1;name of existing variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;file name;Input;literal;Literal
;;;;;3;format descriptor;Input;literal;Literal
variable to json;Function;1;2;1
;;;;;1;name of existing variable;Code piece;left hand expression;Code piece or :literal: e.g. variable or table reference
;;;;;2;json text;Input;literal;Literal
variance;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
variance if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
variance if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
variance ignore blanks;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
variance ignore blanks if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
variance ignore blanks if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
variance ignore zero;Function;0;99999999;1
;;;;;1;values;Input;valid;Numeral, Literal, Boolean, Date, Parameter
variance ignore zero if;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
variance ignore zero if any;Function;3;99999999;2
;;;;;1;values to calculate;Input;parameters;Parameter sets
;;;;;2;correponding values to compare;Input;parameters;Parameter sets
;;;;;3;comparison expression;Code piece;comparison expression;Code piece or :literal
vertical;Function;1;1;1
;;;;;1;matrix or parameter set;Input;parameters;Parameter sets
view;Procedure;1;2;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;browser;Input;literal;Literal
view pause;Procedure;1;2;1
;;;;;1;table name;Input;literal;Literal
;;;;;2;browser;Input;literal;Literal
view reset;Procedure;0;0;1
wait;Procedure;1;2;1
;;;;;1;time in seconds;Input;numeral;Numeral
;;;;;2;prompt;Input;literal;Literal
wait countdown;Both;1;2;1
;;;;;1;time in seconds;Input;numeral;Numeral
;;;;;2;prompt;Input;literal;Literal
wait until;Procedure;1;2;1
;;;;;1;target date / time;Input;to date;Values which are dates or can be converted to dates
;;;;;2;prompt;Input;literal;Literal
wait until countdown;Both;1;2;1
;;;;;1;target date / time;Input;to date;Values which are dates or can be converted to dates
;;;;;2;prompt;Input;literal;Literal
watch continue;Both;0;0;1
watch read;Both;0;0;1
watch reset;Both;0;0;1
watch start;Both;0;0;1
watch stop;Both;0;0;1
web docs;Procedure;0;2;1
;;;;;1;keyword;Input;literal;Literal
;;;;;2;browser;Input;literal;Literal
week;Function;1;2;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
;;;;;2;options;Input;numeral;Numeral
weekday;Function;1;2;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
;;;;;2;weekday calculation variant;Input;numeral;Numeral
while;Procedure;1;1;1
;;;;;1;boolean expression;Input;boolean;Boolean type
whole;Function;1;1;1
;;;;;1;value;Input;numeral;Numeral
with table;Procedure;2;2;1
;;;;;1;name of existing table;Input;literal;Literal
;;;;;2;row number;Input;numeral or literal;Literal version of numeral created
working directory;Both;0;1;1
;;;;;1;directory name;Input;literal;Literal
write accessible;Function;1;1;1
;;;;;1;directory name;Input;literal;Literal
write accessible repeat if locked;Function;1;1;1
;;;;;1;directory name;Input;literal;Literal
xnor;Function;0;99999999;1
;;;;;1;values;Input;boolean;Boolean type
xor;Function;0;99999999;1
;;;;;1;values;Input;boolean;Boolean type
year;Function;1;1;1
;;;;;1;date;Input;to date;Values which are dates or can be converted to dates
zip check file;Procedure;1;1;1
;;;;;1;archive file name;Input;literal;Literal
zip compress;Procedure;1;3;1
;;;;;1;source path name;Input;literal;Literal
;;;;;2;archive file name;Input;literal;Literal
;;;;;3;archive type;Input;literal;Literal
zip extract all;Procedure;1;2;1
;;;;;1;archive file name;Input;literal;Literal
;;;;;2;target directory name;Input;literal;Literal
zip extract files;Procedure;2;3;1
;;;;;1;archive file name;Input;literal;Literal
;;;;;2;content file names;Input;to parameters members to literal;Valid types are converted to parameters, members to literal
;;;;;3;target directory name;Input;literal;Literal
