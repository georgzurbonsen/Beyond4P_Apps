#!/usr/local/bin/Beyond4P


include ( Support Library );


// 	I have moved the code into "Support Library.b4p"

//	Avilable functions
//	view ( table name [, browser name] )
//	view reset;
//	view pause ( table name [, browser name] )



table load ( cities, Cities.csv );
view pause( cities, chrome ); // or brave, ... 

view(cities, excel);


interactive;
// view reset;
