
{
	echo("Test loading XML files");
	table load( t, XMLfile.xml, XML );
	table save( t, XMLfile.csv );

}

/*
	table insert columns( t, section, "", '>XML Attributes' );

	section[] = '';
	table process( t, if ([XML Tag]==section & [XML Usage]==Start) section[] = [name]; if ([XML Usage]==Empty) [section] = section[] );

	table save( t, xmlfile_out.csv );
	interactive;
*/

